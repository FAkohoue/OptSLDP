% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{filter_snps_by_maf}
\alias{filter_snps_by_maf}
\title{Filter SNPs by minor allele frequency}
\usage{
filter_snps_by_maf(snp_info, geno_mat, maf_threshold = 0.05, ctx = NULL)
}
\arguments{
\item{snp_info}{Marker metadata \code{data.table}.}

\item{geno_mat}{Numeric genotype matrix \strong{or} \code{NULL} (GDS path).}

\item{maf_threshold}{Minimum MAF. Default \code{0.05}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}
}
\value{
A named list:
\describe{
\item{\code{snp_info}}{Filtered metadata, preserving original order.}
\item{\code{geno_mat}}{Filtered matrix (\code{NULL} for GDS path).}
\item{\code{maf_table}}{\code{data.table} of SNP / AF / MAF for retained markers.}
}
}
\description{
Removes SNPs whose MAF is below \code{maf_threshold} or is \code{NA}. When a GDS
context is active the filtering is performed entirely in the GDS layer
without loading genotypes into RAM; when in-memory, the matrix is subset
in place.
}
