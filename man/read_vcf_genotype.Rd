% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_vcf_genotype}
\alias{read_vcf_genotype}
\title{Read VCF genotype data}
\usage{
read_vcf_genotype(
  file,
  vcf_snp_threshold = 50000L,
  clean_malformed = FALSE,
  gds_dir = tempdir(),
  n_cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to a \code{.vcf} or \code{.vcf.gz} file.}

\item{vcf_snp_threshold}{SNP count above which the SNPRelate path is used.
Default \code{50000}.}

\item{clean_malformed}{Logical.  If \code{TRUE}, stream-clean the VCF before
conversion to remove lines with unexpected column counts.  Adds one extra
pass over the file but is necessary for some variant callers.
Default \code{FALSE}.}

\item{gds_dir}{Directory for the temporary GDS file.
Default \code{tempdir()}.}

\item{n_cores}{Threads passed to \code{snpgdsGetGeno()}.  Default \code{1}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
Named list: \code{snp_info}, \code{geno_mat}, \code{sample_ids}, \code{format}.
}
\description{
Reads a VCF or bgzipped VCF and converts GT fields to additive dosage
(0/1/2/NA).  Three code paths are selected automatically:
}
\details{
\itemize{
\item \strong{Small VCF} (fewer than \code{vcf_snp_threshold} variants, default 50 000):
\code{VariantAnnotation::readVcf()} in one pass.
\item \strong{Large VCF} (>= \code{vcf_snp_threshold} variants): \code{SNPRelate::snpgdsVCF2GDS()}
streams the VCF to a temporary GDS binary, then dosage is read
chromosome-by-chromosome.  Peak RAM equals one chromosome of dosage.
\item \strong{Large VCF with cleaning} (\code{clean_malformed = TRUE}): before conversion,
\code{clean_vcf()} streams through the file and removes any lines whose
tab-column count does not match the header.  Needed for VCF files from
NGSEP and some other callers that embed "/" in FORMAT fields.
}

All three paths return an identical list so the rest of the pipeline is
unaffected.
}
