% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{find_ld_neighbors}
\alias{find_ld_neighbors}
\title{Find LD neighbors for a focal SNP}
\usage{
find_ld_neighbors(
  focal_snp,
  geno_mat,
  candidate_ids,
  r2_threshold = 0.9,
  ctx = NULL
)
}
\arguments{
\item{focal_snp}{SNP ID of the focal marker.}

\item{geno_mat}{Numeric genotype matrix \strong{or} \code{NULL} (GDS path).}

\item{candidate_ids}{Character vector of SNP IDs to evaluate (may include
\code{focal_snp}; it is always excluded from the result).}

\item{r2_threshold}{Minimum r^2 for neighbor classification. Default \code{0.90}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}
}
\value{
Character vector of neighbor SNP IDs (excluding \code{focal_snp}).
}
\description{
Returns all SNPs from \code{candidate_ids} whose pairwise r^2 with \code{focal_snp}
meets or exceeds \code{r2_threshold}. Scale-aware: uses GDS streaming when a
context with \code{strategy == "gds"} is supplied.
}
\examples{
\dontrun{
nbrs <- find_ld_neighbors("rs100", geno_mat, window_snps, r2_threshold = 0.90)
nbrs <- find_ld_neighbors("rs100", NULL,     window_snps, r2_threshold = 0.90,
                          ctx = gds_ctx)
}
}
