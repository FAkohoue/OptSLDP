% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pruning.R
\name{prune_background_snps}
\alias{prune_background_snps}
\title{LD-prune background (non-important) SNPs chromosome-wise}
\usage{
prune_background_snps(
  remaining_snps,
  snp_info,
  geno_mat,
  r2_genome = 0.8,
  slide_max_bp = 1000000L,
  ctx = NULL,
  verbose = TRUE
)
}
\arguments{
\item{remaining_snps}{Character vector of non-protected SNP IDs to prune.}

\item{snp_info}{Marker metadata \code{data.table}.}

\item{geno_mat}{Numeric genotype matrix \strong{or} \code{NULL} (GDS path).}

\item{r2_genome}{Pruning r^2 threshold. Default \code{0.80}.}

\item{slide_max_bp}{Sliding window in bp for the GDS path. Default
\verb{1 000 000}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Character vector of retained SNP IDs.
}
\description{
Applies greedy LD pruning to all SNPs outside the protected set.
}
\details{
\itemize{
\item \strong{In-memory} (\code{strategy = "in_memory"} / \code{"chunked"}): loads the
chromosome sub-matrix and applies a forward-selection greedy loop.
\item \strong{GDS} (\code{strategy = "gds"}): delegates to \code{snpgdsLDpruning()} which
processes genotypes in streaming blocks -- the chromosome sub-matrix is
never fully materialised in RAM.
}
}
