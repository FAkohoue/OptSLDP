% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{clean_genotype_file}
\alias{clean_genotype_file}
\title{Clean a malformed genotype file by removing lines with wrong column counts}
\usage{
clean_genotype_file(
  file_in,
  file_out,
  sep = "auto",
  chunk_size = 50000L,
  verbose = TRUE
)
}
\arguments{
\item{file_in}{Path to the input genotype file (plain or \code{.gz}).}

\item{file_out}{Path for the cleaned output file.  If it ends in \code{.gz}
the output is gzip-compressed.}

\item{sep}{Field separator: \code{","}, \code{"\\t"}, or \code{"auto"} (default).
\code{"auto"} detects from the file extension (\code{vcf} / \code{hmp} -> tab,
everything else -> comma).}

\item{chunk_size}{Lines per chunk. Default \code{50000}.}

\item{verbose}{Print progress every 500 000 data lines. Default \code{TRUE}.}
}
\value{
Invisibly: a named integer vector with elements \code{total}, \code{removed},
and \code{kept}.
}
\description{
Streams through any accepted genotype file format (numeric CSV, HapMap,
VCF / bgzipped VCF) in 50 000-line chunks and writes only lines whose
delimiter-separated column count matches the header to a new output file.
The separator is detected automatically from the file extension and the
first non-comment header line:
}
\details{
\tabular{ll}{
   Format \tab Separator \cr
   VCF \tab tab \cr
   HapMap \tab tab \cr
   Numeric \tab comma \cr
}


VCF comment lines (starting with \verb{#}) are always passed through unchanged;
the column count is established from the \verb{#CHROM} line.  For numeric and
HapMap files the first line is the header and sets the expected count.
}
