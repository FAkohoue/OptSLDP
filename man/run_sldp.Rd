% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sldp.R
\name{run_sldp}
\alias{run_sldp}
\title{Run the Selective Linkage Disequilibrium Pruning (SLDP) pipeline}
\usage{
run_sldp(
  genotype_file,
  phenotype_file,
  output_file,
  sample_col = "Sample",
  trait_col = "Trait1",
  covar_cols = NULL,
  format = c("auto", "numeric", "hapmap", "vcf"),
  output_format = c("numeric", "hapmap"),
  mode = c("A", "B", "C"),
  scale_strategy = NULL,
  gds_dir = tempdir(),
  n_cores = max(1L, parallel::detectCores() - 1L),
  maf_threshold = 0.05,
  preprune_large = TRUE,
  preprune_r2 = 0.99,
  pval_threshold = NULL,
  z_threshold = NULL,
  pve_threshold = NULL,
  threshold_logic = c("AND", "OR"),
  window_kb = 50,
  include_ld_neighbors = TRUE,
  r2_flag = 0.9,
  r2_genome = 0.8,
  slide_max_bp = 1000000L,
  stats_output_file = NULL,
  summary_output_file = NULL,
  clean_malformed = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{genotype_file}{Path to genotype file (numeric CSV, HapMap, VCF).}

\item{phenotype_file}{Path to phenotype file.  Must contain a sample ID
column (\code{sample_col}) and one or more numeric trait columns (\code{trait_col}).}

\item{output_file}{Path for the pruned output genotype file.}

\item{sample_col}{Sample ID column in the phenotype file.
Default \code{"Sample"}.}

\item{trait_col}{Trait column name(s).  A single string or a
character vector for multi-trait analysis.  Default \code{"Trait1"}.}

\item{covar_cols}{Covariate column name(s) in the phenotype file.
Covariates are shared across all traits.  \code{NULL} = no covariates.}

\item{format}{Genotype format: \code{"auto"} (default), \code{"numeric"},
\code{"hapmap"}, or \code{"vcf"}.}

\item{output_format}{Output format: \code{"numeric"} (default) or
\code{"hapmap"}.}

\item{mode}{Candidate selection mode: \code{"A"} (p-value),
\code{"B"} (effect-size), or \code{"C"} (hybrid).}

\item{scale_strategy}{Override auto-detection: \code{"in_memory"},
\code{"chunked"}, or \code{"gds"}.  \code{NULL} = auto.}

\item{gds_dir}{Directory for intermediate GDS files.
Default \code{tempdir()}.}

\item{n_cores}{Threads for GDS operations.
Default \code{max(1, detectCores() - 1)}.}

\item{maf_threshold}{Minimum MAF. Default \code{0.05}.}

\item{preprune_large}{Apply high-LD pre-pruning. Default \code{TRUE}.}

\item{preprune_r2}{r^2 threshold for pre-pruning. Default \code{0.99}.}

\item{pval_threshold}{P-value upper bound (modes A / C).}

\item{z_threshold}{|z-score| lower bound (modes B / C).}

\item{pve_threshold}{PVE lower bound (modes B / C).}

\item{threshold_logic}{\code{"AND"} (default) or \code{"OR"}.}

\item{window_kb}{Positional expansion window in kb. Default \code{50}.}

\item{include_ld_neighbors}{Include LD neighbours. Default \code{TRUE}.}

\item{r2_flag}{r^2 threshold for neighbour inclusion. Default
\code{0.90}.}

\item{r2_genome}{r^2 threshold for background pruning. Default
\code{0.80}.}

\item{slide_max_bp}{Sliding window (bp) for GDS pruning. Default
\verb{1 000 000}.}

\item{stats_output_file}{Optional path for pruning statistics CSV.}

\item{summary_output_file}{Optional path for plain-text summary.}

\item{clean_malformed}{If \code{TRUE}, stream-clean the genotype file
before reading by removing any lines whose column count does not
match the header.  Works for all accepted formats (numeric CSV,
HapMap, VCF).  Needed for files from NGSEP and other callers that
produce malformed lines.  Adds one extra streaming pass.
Default \code{FALSE}.}

\item{verbose}{Print timestamped progress. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{final_snp_info}}{\code{data.table} of retained SNP metadata.}
\item{\code{final_geno_mat}}{Retained genotype matrix (\code{NULL} for GDS runs).}
\item{\code{screening_stats}}{Single \code{data.table} (one trait) \strong{or} named
list of \code{data.table}s (multiple traits).}
\item{\code{candidate_snps_per_trait}}{Named list of per-trait candidate SNP
IDs (only present in multi-trait runs; \code{NULL} otherwise).}
\item{\code{candidate_snps}}{Union of candidate SNPs across all traits (or
the single-trait candidate set).}
\item{\code{important_snps}}{All protected SNP IDs.}
\item{\code{background_retained}}{Retained background SNP IDs.}
\item{\code{pruning_stats}}{Step-by-step count \code{data.table}.}
\item{\code{output_file}}{Path to the written output.}
\item{\code{scale_strategy}}{Strategy used.}
}
}
\description{
End-to-end workflow.  The user supplies exactly \strong{two input files} -- a
genotype file and a phenotype file -- plus an output path.  All internal
steps (phenotype reading, sample alignment, GWAS screening, candidate
selection, important-SNP expansion, background pruning, output writing) are
handled automatically.
}
\section{Multi-trait analysis}{

Set \code{trait_col} to a character vector containing more than one column name
from the phenotype file.  The pipeline then:
\enumerate{
\item Runs screening (marginal regression + residualisation) \strong{separately}
for each trait.
\item Selects candidate SNPs independently for each trait using the shared
threshold settings.
\item Takes the \strong{union} of all per-trait candidate sets before expansion.
\item Expands the union into the protected set (positional window + LD
neighbours) and runs background pruning once on the remainder.
}
The final pruned panel therefore retains every SNP that is important for
\strong{any} of the requested traits.  The return value includes per-trait
screening statistics and per-trait candidate lists alongside the single
shared output file.
}

\section{Scale strategy}{
\tabular{lll}{
   Strategy \tab Trigger (default) \tab LD backend \cr
   \code{in_memory} \tab n_snps <= 200 000 \tab \code{cor()} on full matrix in RAM \cr
   \code{chunked} \tab 200K < n_snps <= 2M \tab \code{cor()} per chromosome in RAM \cr
   \code{gds} \tab n_snps > 2M \tab \code{snpgdsLDMat()} from disk \cr
}


Adjustable via \code{options(optsldp.thresh_small, optsldp.thresh_medium)}.
}

\examples{
\dontrun{
geno_file  <- system.file("extdata", "example_genotypes_numeric.csv",
                           package = "OptSLDP")
pheno_file <- system.file("extdata", "example_phenotype.csv",
                           package = "OptSLDP")

# -- Single trait ------------------------------------------------------------
res <- run_sldp(
  genotype_file  = geno_file,
  phenotype_file = pheno_file,
  output_file    = tempfile(fileext = ".csv"),
  trait_col      = "Trait1",
  mode           = "A",
  pval_threshold = 0.05
)

# -- Multi-trait: union protection -------------------------------------------
res <- run_sldp(
  genotype_file  = geno_file,
  phenotype_file = pheno_file,
  output_file    = tempfile(fileext = ".csv"),
  trait_col      = c("Trait1", "Trait2"),
  covar_cols     = c("PC1", "PC2"),
  mode           = "A",
  pval_threshold = 0.05
)
# Inspect per-trait results
names(res$screening_stats)          # "Trait1" "Trait2"
res$candidate_snps_per_trait$Trait1
}
}
