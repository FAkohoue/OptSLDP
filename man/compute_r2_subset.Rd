% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{compute_r2_subset}
\alias{compute_r2_subset}
\title{Compute pairwise LD (r^2) for a subset of SNPs}
\usage{
compute_r2_subset(geno_mat, snp_ids, ctx = NULL)
}
\arguments{
\item{geno_mat}{Numeric genotype matrix (SNPs x samples, coded 0/1/2/NA)
\strong{or} \code{NULL} when \code{ctx$strategy == "gds"}.  When provided directly
(no \code{ctx}), this is the only data source used.}

\item{snp_ids}{Character vector of SNP IDs. All IDs must be present in
either \code{rownames(geno_mat)} or the GDS file.}

\item{ctx}{Optional GDS context list produced by \code{.build_gds_context()}.
When \code{NULL} (default) the function uses \code{geno_mat} directly.}
}
\value{
Symmetric numeric r^2 matrix with row and column names equal to
\code{snp_ids}.
}
\description{
Dispatches to an in-memory \code{cor()} computation or a disk-backed
\code{snpgdsLDMat()} call depending on the scale strategy embedded in \code{ctx}.
When no context is supplied the function falls back to the pure in-memory
path, preserving backward compatibility for direct calls.
}
\examples{
\dontrun{
# Direct in-memory call (no context needed for small datasets)
r2 <- compute_r2_subset(geno_mat, snp_ids = c("rs1", "rs2", "rs3"))

# Context-aware call (large datasets)
r2 <- compute_r2_subset(NULL, snp_ids = chr_snps, ctx = gds_ctx)
}
}
