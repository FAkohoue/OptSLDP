% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{preprune_high_ld}
\alias{preprune_high_ld}
\title{Chromosome-wise high-LD pre-pruning}
\usage{
preprune_high_ld(
  snp_info,
  geno_mat,
  r2_pre = 0.99,
  slide_max_bp = 500000L,
  ctx = NULL,
  verbose = TRUE
)
}
\arguments{
\item{snp_info}{Marker metadata \code{data.table}.}

\item{geno_mat}{Numeric genotype matrix \strong{or} \code{NULL} (GDS path).}

\item{r2_pre}{High-LD removal threshold. Default \code{0.99}.}

\item{slide_max_bp}{Sliding window in bp for the GDS path. Default \verb{500 000}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A named list with \code{snp_info}, \code{geno_mat} (or \code{NULL}), and
\code{kept_snps}.
}
\description{
Performs a fast pre-pruning pass at a very high LD threshold (default
\code{r^2 >= 0.99}) to remove near-duplicate SNPs before the main SLDP pipeline.
}
\details{
\itemize{
\item \strong{In-memory} (\code{strategy = "in_memory"} / \code{"chunked"}): greedy
position-order loop using \code{compute_r2_subset()}.
\item \strong{GDS} (\code{strategy = "gds"}): uses \code{snpgdsLDpruning()} which processes
genotypes in streaming blocks -- fast enough for 10M+ SNP panels.
}
}
