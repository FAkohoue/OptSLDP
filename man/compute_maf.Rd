% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{compute_maf}
\alias{compute_maf}
\title{Compute allele frequency and minor allele frequency for every SNP}
\usage{
compute_maf(geno_mat, ctx = NULL)
}
\arguments{
\item{geno_mat}{Numeric genotype matrix (SNPs x samples, coded 0/1/2/NA)
\strong{or} \code{NULL} when \code{ctx$strategy == "gds"}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}
}
\value{
A \code{data.table} with columns \code{SNP}, \code{AF}, and \code{MAF}.
When using the GDS path, \code{SNP} values are the SNP IDs stored in the file.
}
\description{
For the in-memory path, estimates AF from the dosage matrix as
\code{rowMeans / 2}. For the GDS path, delegates to \code{snpgdsSNPRateFreq()} which
streams genotypes from disk without loading the full matrix.
}
\examples{
\dontrun{
maf_dt <- compute_maf(geno_mat)
}
}
