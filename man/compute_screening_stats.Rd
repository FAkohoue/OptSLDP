% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screening.R
\name{compute_screening_stats}
\alias{compute_screening_stats}
\title{Compute marginal screening statistics}
\usage{
compute_screening_stats(geno_mat, y, covar = NULL, verbose = TRUE)
}
\arguments{
\item{geno_mat}{Numeric genotype matrix (SNPs x samples, coded 0/1/2/NA).}

\item{y}{Numeric phenotype vector \strong{or} numeric matrix
(n_samples x n_traits) aligned to \code{colnames(geno_mat)}.}

\item{covar}{Optional covariate matrix / data.frame (nrow == nrow(y)).
Applied to every trait independently. Default \code{NULL}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
Single \code{data.table} (vector \code{y}) \strong{or} named list of \code{data.table}s
(matrix \code{y}).  Each table has columns: SNP, beta, SE, z_score, P.value,
PVE, AF, MAF.
}
\description{
Fits a marginal linear regression of the phenotype on each SNP.  When
covariates are supplied, \code{y} is first residualised on them so the scan is
covariate-adjusted.
}
\section{Multi-trait input}{

When \code{y} is a numeric matrix (n_samples x n_traits) the function is called
once per column and returns a \strong{named list} of per-trait \code{data.table}s.
Column names of \code{y} become the names of the list.  When \code{y} is a plain
numeric vector the function returns a single \code{data.table} as before
(backward compatible).
}

\examples{
\dontrun{
# Single trait
stats <- compute_screening_stats(geno_mat, y = pheno_vec)

# Multiple traits -- returns named list
stats_list <- compute_screening_stats(geno_mat, y = pheno_matrix)
stats_list[["Yield"]]
}
}
