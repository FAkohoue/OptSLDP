% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pruning.R
\name{expand_important_snps}
\alias{expand_important_snps}
\title{Expand candidate SNPs into the full set of important (protected) SNPs}
\usage{
expand_important_snps(
  candidate_snps,
  snp_info,
  geno_mat,
  window_kb = 50,
  include_ld_neighbors = TRUE,
  r2_flag = 0.9,
  ctx = NULL,
  verbose = TRUE
)
}
\arguments{
\item{candidate_snps}{Character vector of candidate SNP IDs.}

\item{snp_info}{Marker metadata \code{data.table}.}

\item{geno_mat}{Numeric genotype matrix \strong{or} \code{NULL} (GDS path).}

\item{window_kb}{Positional expansion window in kb (each side).
Default \code{50}.}

\item{include_ld_neighbors}{Logical; include LD neighbors. Default \code{TRUE}.}

\item{r2_flag}{r^2 threshold for neighbor inclusion. Default \code{0.90}.}

\item{ctx}{Optional GDS context list. Default \code{NULL}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Character vector of unique important SNP IDs (superset of
\code{candidate_snps}).
}
\description{
Builds the protected marker set in two layers:
\enumerate{
\item \strong{Positional window} -- every SNP within \code{window_kb} kb of each candidate
(same chromosome) is added.
\item \strong{LD neighborhood} (optional) -- SNPs in the window with r^2 >= \code{r2_flag}
against the focal candidate are also added.
}
}
\details{
The LD computation dispatches to the GDS backend automatically when a GDS
context (\code{ctx}) is supplied.
}
