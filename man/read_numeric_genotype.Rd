% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_numeric_genotype}
\alias{read_numeric_genotype}
\title{Read numeric dosage genotype data}
\usage{
read_numeric_genotype(
  file,
  sep = NULL,
  check_names = FALSE,
  chunk_rows = 50000L,
  chunk_threshold = 200000L,
  clean_malformed = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to a delimited file (\code{.csv}, \code{.txt}, etc.).}

\item{sep}{Field separator. \code{NULL} triggers auto-detection.}

\item{check_names}{Sanitise column names. Default \code{FALSE}.}

\item{chunk_rows}{Rows per chunk for the chunked path. Default \verb{50 000}.}

\item{chunk_threshold}{Row count above which chunked reading is used.
Default \verb{200 000}.}

\item{clean_malformed}{Logical. If \code{TRUE}, stream-clean the file before
reading by removing lines with unexpected column counts. Default \code{FALSE}.}

\item{verbose}{Print progress messages when cleaning. Default \code{TRUE}.}
}
\value{
Named list: \code{snp_info}, \code{geno_mat}, \code{sample_ids}, \code{format}.
}
\description{
Reads a file in the package's standard format:
\verb{SNP, CHR, POS, REF, ALT, sample1, sample2, ...}.
}
\details{
Files with more than \code{chunk_threshold} rows are read in chunks of
\code{chunk_rows} rows so that peak RAM equals one chunk rather than two full
copies of the file.  Smaller files use a single \code{fread()} call.
}
