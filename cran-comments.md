## Test environments
- Local artifact scaffold only (R not available in build container).

## Notes
- This artifact includes a complete package source scaffold, pkgdown files, example data,
  and documentation source. It was not built or checked inside this environment because
  R is unavailable here.
